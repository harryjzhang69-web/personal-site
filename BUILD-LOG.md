# BUILD-LOG · 个人网站完整搭建与踩坑全纪录

> 跟 `STATUS.md`（当前状态速查）配合看：`STATUS.md` 告诉你"现在是什么样"，
> 这份文件告诉你"怎么一步步变成这样的、每次改版/踩坑的真实过程和根因"。

技术栈：纯静态 HTML/CSS/JS，零依赖，设计参考 Linear 风格。

---

## 阶段一：从零搭建（v1.1 ~ v1.9）

**起点**：`df20264ee`（`first commit: harry's personal site`）到 `a462ce2`（`v1.9`）。

**演进路径**（从提交历史还原）：
1. `v1.1` 独立开发者风格：清华徽章 + 产品展示 + 简历时间线
2. `v1.2` 三个真实产品首次上线：坐姿监督员 PostureGuard / 罗盘 Compass / AI 学习宝典
3. `v1.3~v1.4` 打磨细节：hero区两栏布局、单字符Logo、渐变CTA、mockup演示图、
   社交矩阵、SEO TDK；三个产品加真实demo演示
4. `v1.5~v1.8` 定位调整：从"安全"标签转向纯AI定位，hero文案改得更含蓄、更少销售感，
   AI学习宝典重新定位为"刷题软件核心+350题演示"
5. `v1.9` 头像占位卡片，随时可换真实照片

**关键设计原则确立**（沿用至今）：
- 产品橱窗型定位：不是博客，是"产品列表+下载入口+创作过程"的独立开发者展示页
- 每个真实产品都要有可交互的demo演示区，不是纯文字描述

---

## 阶段二：双业务模式重构（v2.0 ~ v3.0）

**触发**：从"纯产品展示"扩展为"ToB咨询 + 独立产品"双轨商业模式。

**关键改动**：
- `v2.0`：新增 ToB AI落地咨询板块 + 独立产品板块并存，加付费/预约钩子
- `v2.1`：ToB板块改成痛点导向文案，加私密预约弹窗，案例展示重新框定
- `v3.0`：hero区改为完整简历型自我介绍 + 背景音乐播放器 + 真实微信二维码 +
  AI学习宝典产品目录接入

**产品持续迭代**：
- `v3.1`：合作方公司匿名化处理，AI学习宝典定价199，商务联系加电话，
  背景音乐首次交互自动尝试播放
- `v3.2`：罗盘 Compass MVP v0.1 首次上线——64卦数据库 + 3种起卦方式（摇钱/报数/时间）
  + AI对话引擎（**注意：这版AI对话是写死脚本演示，没接真实大模型**）
- `v3.3`：移除"创意墙"板块，FIG章节重新编号，全面移动端响应式打磨

---

## 阶段三：罗盘 Compass 独立打磨 + Bug 修复

罗盘作为唯一一个"网页里的独立小产品"，单独经历了好几轮 bug 修复：

- `303a10e` / `aad2ec1`：中文引号嵌套导致按钮无响应的bug，先改用中文括号规避，
  后来在 `main.js` 的 `aiReply` 的 `isSelf` 分支里发现第二处同类嵌套引号bug
- `f6a2dbf`：Compass 重构为完整版 v0.2，加历史记录、重新起卦、复制、打字机效果、
  toast提示、IIFE安全封装（避免全局变量污染）

**这里的技术债（至今未解决，写进 STATUS.md）**：
罗盘的AI对话是4轮固定脚本演示，不是真实大模型；另一个仓库 `fortune-ai` 有真实的
八字/易经计算引擎但没做成网页——两个产品概念高度重合但代码完全不通用，
需要决定"打通合并"还是"各自独立发展"。

---

## 阶段四：读书行动派接入 + GitHub Pages 部署踩坑（最长的一段排查）

**背景**：`book-action-mvp` 作为独立仓库开发的AI读书产品完成后，需要接入个人网站
Case Studies 板块展示。

**第一次接入（`2635a75`）**：新增读书行动派产品卡片，代码提交、push成功。

**踩坑：GitHub Pages 卡在旧版本，十几次push全部没生效**

**现象**：连续十几次 `git push` 后，`https://harryjzhang69-web.github.io/personal-site/`
上的内容完全没更新，一直卡在很早之前的某次成功构建。

**排查过程**：检查 `Last-Modified` 响应头，发现网站内容确实是几周前的旧版本，
说明 GitHub Pages 的自动构建在某次之后就**静默失败**了，但没有任何明显报错提示。

**根因**：这是一个纯 HTML/CSS/JS 项目，GitHub Pages 默认会用 **Jekyll**
（一个 Ruby 静态站点生成器）去处理仓库内容。Jekyল构建对某些文件名/目录结构
有特殊规则（比如下划线开头的文件/目录会被 Jekyll 忽略），当项目结构触发了
Jekyll 的某个隐式规则后，构建开始静默失败，且**没有任何日志/邮件告警**，
站点就停留在最后一次成功构建的版本，此后所有push都"看起来成功"（git层面确实
push成功了）但实际上从来没有真正部署上去。

**修复**（`922dd43`）：在仓库根目录加一个**空文件** `.nojekyll`，告诉 GitHub Pages
跳过 Jekyll 处理，直接把仓库内容当作纯静态文件原样输出。加上后立刻验证
`Last-Modified` 头变成当天日期，内容确认更新到最新。

**这次踩坑的教训（写进STATUS.md反复提醒自己）**：
- 任何纯 HTML/CSS/JS 项目部署到 GitHub Pages，第一件事就应该加 `.nojekyll`，
  不要等构建"静默失败"了才排查
- 以后如果又发现"push了但网站没更新"，第一件事检查 `.nojekyll` 文件是否还在
  仓库根目录（可能被误删、或被某次 `.gitignore` 规则误伤）

**曾经尝试的应急方案（已弃用，仅存档）**：
- 在Jekyll问题修复前，为了让效果立刻可见，把网站临时也部署到AnyDev服务器
  `http://21.91.155.2:8090/`（`python3 -m http.server`），非持久化，服务器重启即挂。
  Jekyll问题修好后不再需要，但进程可能还占用着8090端口没清理。
- README里一度写着"正式方案是EdgeOne Pages"，链接 `harry-site.edgeone.app`，
  但这个链接从第一次提交到最后都只是占位符，从未真正部署成功过——CodeBuddy
  的EdgeOne `deploy_folder` 工具在当时环境下会报错缺少
  `EDGEONE_PAGES_PROJECT_NAME`参数，无法绕过。GitHub Pages修好后不再需要折腾这条路。
  （注：这个EdgeOne项目名的坑，后来在部署"读书行动派"到EdgeOne Pages时，
  换了个思路——直接在项目根目录加 `edgeone.json` 写死项目名——才最终解决，
  详见 `book-action-mvp` 仓库的 `BUILD-LOG.md` 阶段三。）

**读书行动派链接的三次演变**（`460bb4c` → `a4f38ff` → `9ea57e8`）：
1. 一开始按钮指向已失效的localtunnel链接（`harry-product.loca.lt`），修复为
   AnyDev内网直连IP `http://21.91.155.2:5800/`
2. 后改为"GitHub联动模式"：页面内嵌 `products/book-action/index.html` 子页面，
   通过CORS跨域调用AnyDev后端API
3. 最终（2026-07-10）确认内网IP从来没有真正对外公开过（三个公网检测节点全部
   连接超时），"读书行动派"后端整体迁移到 EdgeOne Pages 后，按钮改为直接跳转
   `https://farrahli-59gv2jsp.edgeone.app/`（不再走内嵌子页面+CORS这条路，
   直接外链到独立部署的公网服务）

---

## 阶段五：真实身份/头像接入

`738f229`：接入真实头像（清华大学乡村振兴工作站赴黑龙江方正支队照片），
角标改为"清华硕士·AI PM"——从这次起，网站头像从占位符换成了真实身份信息。

`06f652b`：背景音乐CDN链接失效，改用本地mp3文件（陶喆《飞行计划》/《再飞行》），
并给播放失败加更清晰的toast提示。

---

## 关键文件/配置速查

| 内容 | 位置 |
|---|---|
| 正式对外链接 | `https://harryjzhang69-web.github.io/personal-site/` |
| 部署方式 | GitHub Pages，`git push` 后1-2分钟自动更新 |
| **必须存在的文件** | 仓库根目录 `.nojekyll`（空文件，跳过Jekyll处理，缺失会导致构建静默失败） |
| 罗盘Compass技术债 | AI对话是写死脚本，未接真实大模型；与独立仓库`fortune-ai`概念重合但代码不通用 |
| AI学习宝典下载区 | `products/ai-bible/`，跟独立仓库`ai-study-app`是同一产品的两份存档，需同步更新 |
| 读书行动派按钮 | 指向 `https://farrahli-59gv2jsp.edgeone.app/`（独立仓库`book-action-mvp`部署在EdgeOne） |
| 已知未解决问题 | 罗盘AI对话未接真实模型；AI学习宝典两处存档容易不同步 |

## 日常更新标准流程

```powershell
cd c:\Users\Harryjzhang\CodeBuddy\Claw\personal_site
git add .
git commit -m "说明改了什么"
git push
```
1-2分钟后自动生效。**别删 `.nojekyll` 文件**，这是唯一一个"删了会导致整站
静默失效但没有任何报错提示"的关键文件。
